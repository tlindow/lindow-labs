## Cursor Cloud specific instructions

This is a **GitHub profile README repository** (`tlindow/tlindow`). The repo's sole purpose is to host a `README.md` that GitHub renders on the user's profile page.

- **No dependencies, build system, or services** — the repo contains only `README.md`.
- **No tests or linting** — there are no automated checks configured.
- **No update script needed** — nothing to install or build.
- Edits to `README.md` are the only meaningful changes in this repo; they take effect once pushed to `main` on GitHub.
